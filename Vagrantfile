# =============================================================================
# Vagrantfile RACINE - Template non utilise
# =============================================================================
#
# Ce fichier est le template Vagrantfile genere par "vagrant init".
# Il n'est PAS utilise par le projet Inception of Things.
#
# Les vrais Vagrantfiles sont dans :
#   p1/Vagrantfile  → 2 VMs (K3s server + K3s worker)
#   p2/Vagrantfile  → 1 VM  (K3s server + 3 apps + Ingress)
#
# P3 n'utilise pas Vagrant du tout : il tourne directement sur la machine
# avec K3d (K3s dans Docker), sans VM intermediaire.
#
# =============================================================================

Vagrant.configure("2") do |config|
  config.vm.box = "base"
end
